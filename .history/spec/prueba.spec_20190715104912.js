/* eslint-disable no-undef */
describe('Prueba 1', () => {
  it('should be zero', () => {
    const foo = 0
    expect(foo).toBe(0)
  })
  it('should be equal to zero', () => {
    const foo = 0
    expect(foo).toBe('0')
  })
})
